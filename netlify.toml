[build]
  publish = "site"
  command = "pip3 install --upgrade pip && pip3 install -r requirements.txt && mkdocs build"

[build.environment]
  PYTHON_VERSION = "3.9"
  PIP_UPGRADE_STRATEGY = "only-if-needed"

# Deploy previews for pull requests
[context.deploy-preview]
  command = "pip3 install --upgrade pip && pip3 install -r requirements.txt && mkdocs build"

# Branch deploys (optional)
[context.branch-deploy]
  command = "pip3 install --upgrade pip && pip3 install -r requirements.txt && mkdocs build"

# Redirect settings (optional)
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404